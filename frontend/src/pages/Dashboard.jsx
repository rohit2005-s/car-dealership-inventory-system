// TODO (Phase 6): fetch vehicles via vehicleService, render search filters,
// VehicleCard grid, pagination, loading/empty states.
export default function Dashboard() {
  return (
    <div className="min-h-screen p-6">
      <h1 className="text-xl font-semibold">Home Dashboard (placeholder)</h1>
    </div>
  );
}
