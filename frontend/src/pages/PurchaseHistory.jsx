// TODO (Phase 6): fetch the logged-in user's purchase history and render it as a list/table.
export default function PurchaseHistory() {
  return (
    <div className="min-h-screen p-6">
      <h1 className="text-xl font-semibold">Purchase History (placeholder)</h1>
    </div>
  );
}
