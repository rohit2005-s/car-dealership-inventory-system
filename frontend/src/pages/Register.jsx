// TODO (Phase 6): build the registration form (name, email, password),
// call useAuth().register, show toast on error, redirect to "/" on success.
export default function Register() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <h1 className="text-xl font-semibold">Register Page (placeholder)</h1>
    </div>
  );
}
